interface:
  display_name: "Ghrouter"
  short_description: "Repo-specific patterns and workflows for ghrouter"
  default_prompt: "Use the ghrouter repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true